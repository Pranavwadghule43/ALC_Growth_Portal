import { useQuery } from '@tanstack/react-query'
import { api } from '../lib/api'
import { ErrorState, formatDate, Loading, PageHeader } from '../components/ui'
interface ChallengeData{period_start:string;period_end:string;targets:Record<string,number>;achieved:Record<string,number>;source:string}
export default function Challenge(){const q=useQuery({queryKey:['challenge'],queryFn:()=>api.get<ChallengeData>('/portal/challenge')});if(q.isLoading)return <Loading/>;if(q.error||!q.data)return <ErrorState error={q.error}/>;return <><PageHeader title="30-Day Collaboration Challenge" description={`${formatDate(q.data.period_start)} – ${formatDate(q.data.period_end)} · ${q.data.source}`}/><div className="grid gap-5 md:grid-cols-2">{Object.entries(q.data.targets).map(([key,target])=>{const achieved=q.data.achieved[key]??0;return <section key={key} className="panel p-6"><div className="flex items-center justify-between"><h2 className="text-lg font-bold capitalize text-navy">{key}</h2><span className="text-2xl font-bold">{achieved}<small className="text-sm font-normal text-slate-500"> / {target}</small></span></div><div className="mt-4 h-3 overflow-hidden rounded-full bg-slate-100"><div className="h-full bg-teal" style={{width:`${Math.min(100,achieved/target*100)}%`}}/></div><p className="mt-2 text-xs text-slate-500">{Math.round(achieved/target*100)}% of target</p></section>})}</div></>}