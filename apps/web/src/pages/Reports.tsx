import { useState } from 'react'
import { Download } from 'lucide-react'
import { api } from '../lib/api'
import { PageHeader } from '../components/ui'
export default function Reports(){const [filters,setFilters]=useState({date_from:'',date_to:'',status:'',activity_type:'',ecosystem:''});const params=new URLSearchParams(Object.entries(filters).filter(([,v])=>v));return <><PageHeader title="Reports" description="Export filtered operational and verified performance data."/><section className="panel p-6"><h2 className="font-bold text-navy">Activity report</h2><p className="mt-1 text-sm text-slate-500">Includes ALC, status, learner reach, leads, and admissions.</p><div className="mt-5 grid gap-4 md:grid-cols-2 xl:grid-cols-5"><div><label>From</label><input className="mt-1" type="date" value={filters.date_from} onChange={e=>setFilters({...filters,date_from:e.target.value})}/></div><div><label>To</label><input className="mt-1" type="date" value={filters.date_to} onChange={e=>setFilters({...filters,date_to:e.target.value})}/></div><div><label>Status</label><select className="mt-1" value={filters.status} onChange={e=>setFilters({...filters,status:e.target.value})}><option value="">All</option>{['SUBMITTED','CORRECTION_REQUIRED','RESUBMITTED','VERIFIED','REJECTED'].map(x=><option key={x}>{x}</option>)}</select></div><div><label>Activity type</label><input className="mt-1" value={filters.activity_type} onChange={e=>setFilters({...filters,activity_type:e.target.value})}/></div><div><label>Ecosystem</label><input className="mt-1" value={filters.ecosystem} onChange={e=>setFilters({...filters,ecosystem:e.target.value})}/></div></div><a className="btn-primary mt-5" href={api.downloadUrl(`/admin/reports/activities.csv?${params}`)}><Download className="h-4 w-4"/>Download CSV</a></section><div className="mt-5 grid gap-4 md:grid-cols-3">{['Verification status','Learner reach','Partner and challenge progress'].map(x=><section key={x} className="panel p-5"><h3 className="font-semibold text-navy">{x}</h3><p className="mt-2 text-sm text-slate-500">Use the activity export with filters, or view the corresponding live portal section.</p></section>)}</div></>}

